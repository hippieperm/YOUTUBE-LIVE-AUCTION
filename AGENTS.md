## 1. 기본 작업 프로세스 (작업 요청 시)

- **main 브랜치 직접 작업**: 별도의 새 브랜치를 생성하지 않고 **`main` 브랜치에서 바로 작업을 진행**합니다.
- **커밋·푸시 실행 조건**: 사용자가 커밋 또는 푸시를 명시적으로 요청한 경우에만 AI가 직접 실행합니다. 별도 요청이 없으면 작업 결과를 로컬 수정 상태로 유지합니다.
- **커밋 전 변경사항 전수 확인**: 커밋·푸시 요청을 받으면 먼저 `git status --short`, 추적 파일의 staged/unstaged diff, 비추적 파일 목록과 내용을 모두 확인합니다. 기존 사용자 작업을 임의로 되돌리거나 덮어쓰지 않으며, 범위가 불명확한 변경사항이 있으면 커밋 전에 사용자에게 확인합니다.
- **현재 로컬 변경사항 반영**: 사용자가 “현재 변경사항 전부 커밋·푸시”라고 요청한 경우, 전수 확인을 마친 뒤 현재 로컬 변경사항을 빠짐없이 하나의 커밋에 반영하고 `main`에 푸시합니다.
- **Codex 앱 커밋·푸시 버튼 사용 시에도 동일하게 적용**: Codex 앱에서 커밋 및 푸시 버튼을 누를 때 생성되는 커밋 메시지는 반드시 **Conventional Commits 형식의 한국어 메시지**로 작성합니다.

## 2. 커밋 메시지 및 명령어 규칙

- 작업 변경 내역을 명확하고 직관적으로 반영하는 Conventional Commits 형식으로 작성합니다.
- **설명은 반드시 한글로 작성합니다.**
- 커밋 메시지는 변경된 파일과 기능을 확인한 뒤, 제목만으로도 핵심 변경을 알 수 있게 작성하고 본문에 주요 변경 내용과 검증 결과를 자세히 기록합니다.
- **실제 줄바꿈 보장**: 커밋 메시지 본문에 `\\n` 문자열을 직접 넣지 않습니다. GitHub에서 `\\n`이 문자 그대로 표시되지 않도록, 실제 개행이 포함된 메시지 파일을 `git commit -F`로 전달하거나 제목과 본문을 각각 `git commit -m` 옵션으로 나누어 전달합니다.
- 커밋 메시지 작성 전후에 `git log -1 --format=%B`로 실제 저장된 메시지를 확인하며, 출력에 리터럴 `\\n`이 있으면 푸시하지 않고 올바른 개행으로 커밋을 수정합니다.
- 사용자가 커밋·푸시를 명시적으로 요청한 경우, 커밋 전에 최종 변경 파일과 커밋 메시지를 사용자에게 먼저 보여주고 실행합니다. 단, 사용자가 실행까지 명확히 요청한 경우에는 확인을 반복하지 않습니다.
- 커밋 후 `git status --short`, 커밋 로그, 원격 `main` 푸시 결과를 확인하고 성공 여부를 보고합니다.

- **타입 분류**:
  - `feat(...)`: 새로운 기능 또는 프리셋 추가
  - `fix(...)`: 버그 수정
  - `tune(...)` / `refactor(...)`: 파라미터 조절 및 코드 개선
  - `style(...)`: UI 스타일링 및 레이아웃 수정
  - `docs(...)`: 문서 및 작업 규칙 수정

## 3. 답변 문체

- 기본 말투는 존댓말이 아닌 **간결한 메모체·상태 보고체**로 작성함.
- 문장 종결은 `~함`, `~중`, `~임`, `~가능`, `~필요`, `~없음`, `~완료`를 우선 사용함.
- `~한다`, `~입니다`, `~좋아`, `~이야`, `물론이지` 같은 대화체·존댓말·불필요한 인사말은 사용하지 않음.
- 완전한 문장보다 핵심 상태, 결과, 다음 행동을 우선해 작성함.
- 사용자의 요청을 반복하거나 같은 내용을 다른 표현으로 반복하지 않음.
- 사용자가 자연스러운 문장, 공식 문서, 홍보문, 번역문을 요청한 경우에는 해당 요청의 문체를 따름.

예시:

- `현재 관련 파일 확인 중`
- `원인은 초기화 순서 문제로 보임`
- `수정 완료. 빌드 검증 필요`
- `이 방식이 더 적합함`

