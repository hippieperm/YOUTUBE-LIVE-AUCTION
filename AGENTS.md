# FILM GRAIN STUDIO - AI 작업 규칙 (Workflow Rules)

## 1. 기본 작업 프로세스 (작업 요청 시)

- **main 브랜치 직접 작업**: 별도의 새 브랜치를 생성하지 않고 **`main` 브랜치에서 바로 작업을 진행**합니다.
- **임의 실행 금지**: AI가 직접 커밋 및 푸시를 실행하지 않습니다. (로컬 수정 상태 유지)
- **작업 완료 시 커밋/푸시 명령어 작성**: 작업이 끝나면 사용자가 직접 복사하여 실행할 수 있도록 **Git 커밋 및 푸시 명령어 코드 블록**을 항상 작성해 제공합니다.
- **Codex 앱 커밋·푸시 버튼 사용 시에도 동일하게 적용**: Codex 앱에서 커밋 및 푸시 버튼을 누를 때 생성되는 커밋 메시지는 반드시 **Conventional Commits 형식의 한국어 메시지**로 작성합니다.

## 2. 커밋 메시지 및 명령어 규칙

- 작업 변경 내역을 명확하고 직관적으로 반영하는 Conventional Commits 형식으로 작성합니다.
- **설명은 반드시 한글로 작성합니다.**
- 작업 완료 시 아래와 같이 복사하여 바로 실행할 수 있는 형태의 명령어 블록을 제공합니다:

  ```bash
  git add .
  git commit -m "<타입>(<스코프>): <한글 작업 설명>"
  git push origin main
  ```

- **타입 분류**:
  - `feat(...)`: 새로운 기능 또는 프리셋 추가
  - `fix(...)`: 버그 수정
  - `tune(...)` / `refactor(...)`: 파라미터 조절 및 코드 개선
  - `style(...)`: UI 스타일링 및 레이아웃 수정
  - `docs(...)`: 문서 및 작업 규칙 수정
